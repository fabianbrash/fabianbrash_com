apiVersion: autoscaling/v1
Kind: HorizontalPodAutoscaler
metadata:
  name: wp-nfs
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: wp-nfs
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80
